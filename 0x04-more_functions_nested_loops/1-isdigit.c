#include "main.h"

/**
 * _isdigit - Check for a digit (0 through 9)
 * @c: Is the is the digit to be checked
 *
 * Return: 1 if c is a digit, 0 therwise
 */

int _isdigit(int c)
{
if (c >= 48 && c <= 57)
{
return (1);
}
{
return (0);
}
}
